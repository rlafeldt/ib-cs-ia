import 'package:flutter/material.dart';

class ClaimDatabaseScreen extends StatelessWidget {
  const ClaimDatabaseScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Claims Database"));
  }
}
