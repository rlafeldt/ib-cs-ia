import 'package:flutter/material.dart';

class ContractDatabaseScreen extends StatelessWidget {
  const ContractDatabaseScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Contract Database"));
  }
}
